# Copy to `.env` and adjust. Docker Compose loads `.env` automatically for ${HOST_IP} in docker-compose.yaml.
# Use the host's LAN IP or hostname if you open Prefect from other machines; use 127.0.0.1 for localhost only.
HOST_IP=127.0.0.1
